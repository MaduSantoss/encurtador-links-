# 🔗 Encurtador de Links (URL Shortener)

Ferramenta de linha de comando (CLI) desenvolvida em Python que transforma URLs longas e complexas em links curtos e fáceis de compartilhar, utilizando a API do serviço **TinyURL**.

## 🚀 Funcionalidades

- **Encurtamento Rápido:** Conexão direta com a API do TinyURL para gerar links instantaneamente.
- **Interface Simples:** Execução via terminal, focada na usabilidade.
- **Requisições HTTP:** Demonstração prática do uso da biblioteca `requests` para comunicação web.

## 🛠️ Tecnologias Utilizadas

- **Python 3.13**
- **Biblioteca `requests`** (Para fazer a ponte entre o script e a API)
- **API TinyURL** (Endpoint público)

## 📦 Pré-requisitos

Diferente dos projetos anteriores, este utiliza uma biblioteca externa que precisa ser instalada.

Certifique-se de ter o Python instalado e execute o seguinte comando no terminal para instalar o `requests`:

```bash
pip install requests

```

##⚙️ Como Executar
1. Clone este repositório ou baixe o arquivo `encurtador.py`.
2. Abra o terminal na pasta do projeto.
3. Execute o comando:

```bash
python encurtador.py

```

4. Cole a URL gigante quando solicitado e receba a versão encurtada.

##📝 Exemplo de Uso```text
Cole aqui sua URL gigante: [https://www.google.com/search?q=python+developer+roadmap](https://www.google.com/search?q=python+developer+roadmap)
Encurtando...
Link curto: [http://tinyurl.com/2xt8z8](http://tinyurl.com/2xt8z8)



##👩‍💻 Autora
Desenvolvido durante estudos de Python, com foco em consumo de APIs e automação de tarefas.

```
